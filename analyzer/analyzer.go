package analyzer

import (
        "github.com/sashakoshka/arf/parser"
)

type Analyzer struct {
        
}

func Analyze (
        module *parser.Module,
) (
        warnCount int,
        errorCount int,
        err error,
) {
        // analyzer := Analyzer {
                // 
        // }

        return
}
